# Airflow Plugins

This folder is mounted to `/opt/airflow/plugins`.

The starter repository does not require custom plugins. Add shared operators, sensors, or lineage emitters here only after the core DAG stabilizes.

