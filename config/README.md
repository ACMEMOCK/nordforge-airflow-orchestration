# Airflow Config

This folder is mounted to `/opt/airflow/config`.

Keep environment-specific Airflow config overrides here only when needed. The starter stack intentionally relies on environment variables in `docker-compose.yaml` and `.env` first.

